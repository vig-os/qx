# Generated label artifacts (regenerable from registry + the Rust CLI).
# Per-ID SVGs are high-volume and noisy in diffs.
labels/

# Sheet outputs from tools/sheet.py — regenerable, no need to track.
sheets/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# uv
uv.lock

# macOS
.DS_Store

# Rust
target/
**/*.rs.bk

# Nix dev shell (#35 Phase 3 — flake.nix + .envrc)
.direnv/
result
result-*

# Claude Code session scratch — per-process runtime locks (sessionId/pid)
# and pre-compaction handoff notes. Never meant to be tracked.
.claude/*.lock
HANDOFF.md
