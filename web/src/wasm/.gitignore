# wasm-bindgen build artifacts — produced by `npm run build:wasm`
# (or `wasm-pack build crates/wasm --target web`). Regenerated from
# the Rust crate `crates/wasm/` per ADR-017 strangler-fig step 8.
qx_wasm*
!.gitignore
